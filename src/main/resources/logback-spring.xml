<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Property Settings -->
    <!-- .env 파일에서 FILE_LOG_DIR 환경 변수 값을 주입함 -->
    <!-- (FILE_LOG_DIR 환경 변수가 설정되어 있지 않으면 /tmp/logs/app 경로를 기본값으로 사용) -->
    <property name="LOG_DIR" value="${FILE_LOG_DIR:-/tmp/logs/app}"/>
<!--    <property name="LOG_PATH" value="${LOG_DIR}/app.log"/>-->

    <!-- Application Loggers -->
    <logger name="jdbc" level="OFF"/>
    <logger name="jdbc.connection" level="OFF"/>
    <logger name="jdbc.sqlonly" level="OFF"/>
    <logger name="jdbc.sqltiming" level="DEBUG"/>
    <logger name="jdbc.audit" level="OFF"/>
    <logger name="jdbc.resultset" level="OFF"/>
    <logger name="jdbc.resultsettable" level="INFO"/>

    <!-- Use spring default values. -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console Appender -->
<!--    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">-->
<!--        <layout class="ch.qos.logback.classic.PatternLayout">-->
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
<!--        </layout>-->
<!--    </appender>-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="jdbc.resultsettable" level="ON" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Rolling File Appender -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
<!--        <file>${LOG_PATH}</file>-->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}[%-5level] : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app_%d{yyyy-MM-dd}_%i.log.gz</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Root Logger -->
    <!-- application.yml의 logging.level 설정보다 우선하여 적용됨 -->
    <!-- Local(개발) 프로파일 시 활성화되는 설정 -->
    <springProfile name="local">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ROLLING_FILE"/>
        </root>
        <!-- 개발 시에는 DEBUG 레벨로 상세하게 표시 -->
        <logger name="kr.co.jaehoon.springboottemplate" level="DEBUG"/>
    </springProfile>

    <!-- Prod(운영) 프로파일 시 활성화되는 설정 -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ROLLING_FILE"/>
        </root>
        <!-- 운영 시에는 INFO 레벨 이상만 로깅 -->
        <logger name="kr.co.jaehoon.springboottemplate" level="INFO"/>
    </springProfile>

</configuration>
